Place your offline raster MBTiles file here:

assets/maps/oltrap.mbtiles

The app copies that file into local app storage on first run, then flutter_map
reads the copied file directly for fast offline map tiles.
